var mongodb = require("mongodb");

exports.open = function(db, callback) {

    mongoserver = new mongodb.Server('localhost', 27017, {auto_reconnect: true, journal: true}),
    db_connector = new mongodb.Db(db, mongoserver, {safe:false});

    db_connector.open(callback);
}